worker_processes 1;

events {
  worker_connections 1024;
}

http {
  server {
    listen 80;
    server_name localhost;

    # Redirige todas las solicitudes HTTP a HTTPS
    return 301 https://$host$request_uri;
  }

  server {
    listen 443 ssl;

    # Cloudflare proporciona SSL, no necesitas especificar certificados locales

    # Especifica los protocolos SSL/TLS permitidos
    ssl_protocols TLSv1.2 TLSv1.3;

    # No es necesario especificar ssl_certificate y ssl_certificate_key con Cloudflare
    server_name www.thedothash.com;
    ssl_certificate www.thedothash.com.crt;
    ssl_certificate_key www.thedothash.com.key;
    

    location / {
      root /usr/share/nginx/html;
      index index.html;
    }
  }
}
